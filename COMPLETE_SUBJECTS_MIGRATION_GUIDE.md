# Complete Subjects Table Migration Guide\n\n## 🎯 Current Schema vs Required Schema\n\n### Your Current Schema:\n```sql\nCREATE TABLE `subjects` (\n  `subject_code` varchar(50) NOT NULL,\n  `subject` varchar(100) NOT NULL,\n  `school_id` varchar(11) NOT NULL,\n  `status` enum('Active','Inactive') DEFAULT 'Active',\n  `section` varchar(45) NOT NULL,\n  `sub_section` varchar(50) DEFAULT NULL,\n  `class_code` varchar(50) NOT NULL,\n  KEY `subjects_school_id` (`school_id`),\n  KEY `subjects_section` (`section`),\n  KEY `subjects_status` (`status`),\n  KEY `subjects_school_id_section_status` (`school_id`,`section`,`status`)\n);\n```\n\n### Required Schema (Enhanced Controller):\n```sql\nCREATE TABLE `subjects` (\n  `subject_code` varchar(50) NOT NULL PRIMARY KEY,\n  `subject` varchar(100) NOT NULL,\n  `school_id` varchar(11) NOT NULL,\n  `status` enum('Active','Inactive') DEFAULT 'Active',\n  `section` varchar(45) NOT NULL,\n  `sub_section` varchar(50) DEFAULT NULL,\n  `type` varchar(50) NOT NULL DEFAULT 'core',           -- NEW\n  `is_elective` boolean NOT NULL DEFAULT FALSE,         -- NEW\n  `elective_group` varchar(50) DEFAULT NULL,            -- NEW\n  `weekly_hours` decimal(3,1) DEFAULT 0.0,              -- NEW\n  `branch_id` varchar(20) NOT NULL,                     -- NEW (CRITICAL)\n  `class_code` varchar(50) NOT NULL,\n  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,     -- NEW\n  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, -- NEW\n  -- Additional indexes...\n);\n```\n\n## 📋 Missing Columns to Add\n\n| Column | Type | Default | Required | Purpose |\n|--------|------|---------|----------|----------|\n| `type` | VARCHAR(50) | 'core' | YES | Subject classification |\n| `is_elective` | BOOLEAN | FALSE | YES | Elective flag |\n| `elective_group` | VARCHAR(50) | NULL | NO | Elective grouping |\n| `weekly_hours` | DECIMAL(3,1) | 0.0 | NO | Teaching hours |\n| `branch_id` | VARCHAR(20) | '' | **YES** | **Branch isolation** |\n| `created_at` | TIMESTAMP | NOW() | YES | Creation time |\n| `updated_at` | TIMESTAMP | NOW() | YES | Update time |\n\n## 🚀 Migration Process\n\n### Step 1: Backup Your Database\n```bash\nmysqldump -u username -p database_name > subjects_backup_$(date +%Y%m%d_%H%M%S).sql\n```\n\n### Step 2: Choose Migration Method\n\n#### Option A: Safe Migration (Recommended)\n```sql\n-- Execute: SAFE_MIGRATE_SUBJECTS_TABLE.sql\n-- This checks for existing columns before adding\n```\n\n#### Option B: Direct Migration\n```sql\n-- Execute: MIGRATE_SUBJECTS_TABLE.sql\n-- Use only if you're sure columns don't exist\n```\n\n### Step 3: Update Branch IDs (CRITICAL)\n```sql\n-- Execute: UPDATE_BRANCH_IDS_SUBJECTS.sql\n-- This assigns branch_id values to existing subjects\n```\n\n## 📁 Migration Files\n\n### 1. `SAFE_MIGRATE_SUBJECTS_TABLE.sql`\n- ✅ **Recommended for production**\n- ✅ Checks if columns exist before adding\n- ✅ Safe to run multiple times\n- ✅ Includes all indexes and constraints\n\n### 2. `MIGRATE_SUBJECTS_TABLE.sql`\n- ✅ Direct ALTER TABLE statements\n- ✅ For clean installations\n- ⚠️ Will fail if columns already exist\n\n### 3. `UPDATE_BRANCH_IDS_SUBJECTS.sql`\n- ✅ **CRITICAL follow-up script**\n- ✅ Assigns branch_id to existing subjects\n- ✅ Multiple assignment strategies\n- ✅ Verification queries included\n\n## ⚠️ Critical Requirements\n\n### Branch ID Assignment\n\n**EVERY subject MUST have a branch_id value** for the enhanced controller to work.\n\n#### Strategy 1: Automatic Assignment\n```sql\n-- Assigns first active branch per school\nUPDATE subjects s\nJOIN (\n    SELECT school_id, MIN(branch_id) as default_branch_id\n    FROM school_locations \n    WHERE status = 'Active'\n    GROUP BY school_id\n) sl ON s.school_id = sl.school_id\nSET s.branch_id = sl.default_branch_id\nWHERE s.branch_id IS NULL OR s.branch_id = '';\n```\n\n#### Strategy 2: Manual Assignment\n```sql\n-- For specific schools\nUPDATE subjects SET branch_id = 'BRCH00001' WHERE school_id = 'SCH/1';\nUPDATE subjects SET branch_id = 'BRCH00002' WHERE school_id = 'SCH/2';\n```\n\n## 🔧 Step-by-Step Execution\n\n### 1. Check Current State\n```sql\n-- See current table structure\nDESCRIBE subjects;\n\n-- Count existing subjects\nSELECT COUNT(*) as total_subjects FROM subjects;\n\n-- Check available branches\nSELECT school_id, branch_id, location_name \nFROM school_locations \nWHERE status = 'Active';\n```\n\n### 2. Run Migration\n```sql\n-- Execute the safe migration script\nsource SAFE_MIGRATE_SUBJECTS_TABLE.sql\n```\n\n### 3. Update Branch IDs\n```sql\n-- Execute the branch ID update script\nsource UPDATE_BRANCH_IDS_SUBJECTS.sql\n```\n\n### 4. Verify Migration\n```sql\n-- Check all subjects have branch_id (should return 0)\nSELECT COUNT(*) FROM subjects WHERE branch_id IS NULL OR branch_id = '';\n\n-- Verify table structure\nDESCRIBE subjects;\n\n-- Test a sample query\nSELECT * FROM subjects LIMIT 5;\n```\n\n## ✅ Verification Checklist\n\n- [ ] All new columns added successfully\n- [ ] Primary key exists on subject_code\n- [ ] All indexes created\n- [ ] Type constraint added\n- [ ] All subjects have branch_id values\n- [ ] No subjects with invalid branch_id\n- [ ] Enhanced controller works without errors\n\n## 🧪 Test Enhanced Controller\n\n### Test Subject Creation\n```bash\ncurl -X POST \"http://localhost:34567/subjects\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"query_type\": \"create\",\n    \"subject\": \"Test Subject\",\n    \"section\": \"PRIMARY\",\n    \"class_code\": \"P1A\",\n    \"type\": \"core\",\n    \"branch_id\": \"BRCH00001\"\n  }'\n```\n\n### Test Subject Retrieval\n```bash\ncurl -X GET \"http://localhost:34567/subjects?query_type=select-all\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"x-branch-id: BRCH00001\"\n```\n\n### Test Branch Isolation\n```bash\ncurl -X GET \"http://localhost:34567/subjects/by-section?section=PRIMARY\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"x-branch-id: BRCH00001\"\n```\n\n## 🚨 Troubleshooting\n\n### Common Issues\n\n**1. \"Column already exists\" errors**\n- Use the safe migration script\n- Check which columns already exist\n\n**2. \"Cannot add NOT NULL column\" errors**\n- Add column as nullable first\n- Update values, then modify to NOT NULL\n\n**3. \"Subjects without branch_id\" errors**\n- Run the branch ID update script\n- Verify all subjects have valid branch_id\n\n**4. \"Unknown column 'type'\" still appears**\n- Verify migration completed successfully\n- Check table structure with DESCRIBE\n- Restart application if needed\n\n### Rollback Plan\n\n```sql\n-- Emergency rollback (use with caution)\nALTER TABLE subjects DROP COLUMN type;\nALTER TABLE subjects DROP COLUMN is_elective;\nALTER TABLE subjects DROP COLUMN elective_group;\nALTER TABLE subjects DROP COLUMN weekly_hours;\nALTER TABLE subjects DROP COLUMN branch_id;\nALTER TABLE subjects DROP COLUMN created_at;\nALTER TABLE subjects DROP COLUMN updated_at;\n```\n\n## 🎉 Success Indicators\n\n- ✅ No \"Unknown column\" errors\n- ✅ Enhanced controller operations work\n- ✅ Branch isolation functions properly\n- ✅ Subject creation/update/retrieval work\n- ✅ All subjects have valid branch_id values\n- ✅ New features (electives, types) available\n\n## 📞 Post-Migration\n\n### Update Application\n1. Restart your Node.js application\n2. Test all subject-related operations\n3. Verify branch isolation works\n4. Test new features (electives, types)\n\n### Monitor\n1. Check application logs for errors\n2. Monitor database performance\n3. Verify all operations work as expected\n\n---\n\n**After successful migration, your enhanced subjects controller will be fully functional with all advanced features including branch isolation, elective management, and resilient code generation!**\n"