# Subject Table Migration Guide\n\n## 🎯 Issue\n\n**Error**: `Unknown column 'type' in 'field list'`\n\n**Root Cause**: The enhanced subjects controller expects additional columns in the `subjects` table that don't exist in the current database schema.\n\n## 📋 Missing Columns\n\nThe following columns need to be added to the `subjects` table:\n\n| Column | Type | Default | Description |\n|--------|------|---------|-------------|\n| `type` | VARCHAR(50) | 'core' | Subject type (core, science, art, etc.) |\n| `is_elective` | BOOLEAN | FALSE | Whether subject is elective |\n| `elective_group` | VARCHAR(50) | NULL | Elective group identifier |\n| `weekly_hours` | DECIMAL(3,1) | 0.0 | Weekly teaching hours |\n| `branch_id` | VARCHAR(20) | '' | Branch identifier (CRITICAL) |\n| `created_at` | TIMESTAMP | CURRENT_TIMESTAMP | Creation timestamp |\n| `updated_at` | TIMESTAMP | CURRENT_TIMESTAMP | Update timestamp |\n\n## 🚀 Migration Steps\n\n### Step 1: Backup Your Database\n```bash\nmysqldump -u username -p database_name > backup_before_migration.sql\n```\n\n### Step 2: Run the Safe Migration Script\n\n**Option A: Safe Migration (Recommended)**\n```sql\n-- Execute the contents of ADD_SUBJECT_COLUMNS_SAFE.sql\n-- This checks if columns exist before adding them\n```\n\n**Option B: Direct Migration**\n```sql\n-- Execute the contents of ADD_SUBJECT_COLUMNS.sql\n-- Use only if you're sure columns don't exist\n```\n\n### Step 3: Update Existing Records with Branch IDs\n\n**CRITICAL**: All subjects must have a `branch_id` value.\n\n```sql\n-- Check what branches exist\nSELECT school_id, branch_id, location_name \nFROM school_locations \nWHERE status = 'Active';\n\n-- Update subjects with appropriate branch_id\nUPDATE subjects \nSET branch_id = 'YOUR_BRANCH_ID'  -- Replace with actual branch ID\nWHERE school_id = 'YOUR_SCHOOL_ID' -- Replace with actual school ID\nAND (branch_id IS NULL OR branch_id = '');\n```\n\n### Step 4: Verify Migration\n\n```sql\n-- Check table structure\nDESCRIBE subjects;\n\n-- Verify all subjects have branch_id\nSELECT COUNT(*) as subjects_without_branch_id\nFROM subjects \nWHERE branch_id IS NULL OR branch_id = '';\n\n-- Should return 0\n```\n\n## 📁 Migration Files\n\n### 1. `ADD_SUBJECT_COLUMNS_SAFE.sql`\n- **Purpose**: Safely add missing columns\n- **Features**: Checks if columns exist before adding\n- **Recommended**: Use this for production\n\n### 2. `ADD_SUBJECT_COLUMNS.sql`\n- **Purpose**: Direct column addition\n- **Features**: Simple ALTER TABLE statements\n- **Use Case**: Fresh installations or when sure columns don't exist\n\n### 3. `UPDATE_EXISTING_SUBJECTS_BRANCH_ID.sql`\n- **Purpose**: Update existing records with branch_id values\n- **Features**: Multiple update strategies\n- **Critical**: Must be run after adding columns\n\n## ⚠️ Important Notes\n\n### Branch ID Assignment\n\n**CRITICAL**: Every subject must have a `branch_id` value for the enhanced controller to work.\n\n**Strategies for assigning branch_id:**\n\n1. **Single Branch Schools**: Use the only branch ID\n2. **Multi-Branch Schools**: \n   - Assign subjects to appropriate branches based on location\n   - Use default branch for existing subjects\n   - Review and reassign as needed\n\n### Data Integrity\n\n- **Backup first**: Always backup before migration\n- **Test in staging**: Run migration in test environment first\n- **Verify constraints**: Ensure type values are valid\n- **Check relationships**: Verify branch_id references exist in school_locations\n\n## 🔧 Troubleshooting\n\n### Common Issues\n\n**1. \"Column already exists\" errors**\n- Use the safe migration script\n- Check existing table structure first\n\n**2. \"Cannot add NOT NULL column\" errors**\n- Add column as nullable first\n- Update values\n- Modify to NOT NULL\n\n**3. \"Invalid default value\" errors**\n- Check MySQL version compatibility\n- Adjust timestamp defaults if needed\n\n### Rollback Plan\n\n```sql\n-- If you need to rollback (use with caution)\nALTER TABLE subjects DROP COLUMN type;\nALTER TABLE subjects DROP COLUMN is_elective;\nALTER TABLE subjects DROP COLUMN elective_group;\nALTER TABLE subjects DROP COLUMN weekly_hours;\nALTER TABLE subjects DROP COLUMN branch_id;\nALTER TABLE subjects DROP COLUMN created_at;\nALTER TABLE subjects DROP COLUMN updated_at;\n```\n\n## ✅ Post-Migration Verification\n\n### 1. Test Enhanced Controller\n```bash\n# Test subject creation\ncurl -X POST \"http://localhost:34567/subjects\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"query_type\": \"create\",\n    \"subject\": \"Test Subject\",\n    \"section\": \"PRIMARY\",\n    \"class_code\": \"P1A\",\n    \"type\": \"core\"\n  }'\n```\n\n### 2. Test Subject Queries\n```bash\n# Test subject retrieval\ncurl -X GET \"http://localhost:34567/subjects?query_type=select-all\" \\\n  -H \"Authorization: Bearer <token>\"\n```\n\n### 3. Verify Branch Isolation\n```bash\n# Test branch-specific queries\ncurl -X GET \"http://localhost:34567/subjects/by-section?section=PRIMARY\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"x-branch-id: BRCH00001\"\n```\n\n## 🎉 Success Indicators\n\n- ✅ No \"Unknown column\" errors\n- ✅ All subjects have branch_id values\n- ✅ Enhanced controller operations work\n- ✅ Branch isolation functions properly\n- ✅ Subject creation, update, and retrieval work\n\n## 📞 Support\n\nIf you encounter issues:\n1. Check the error logs\n2. Verify table structure matches the model\n3. Ensure all subjects have branch_id values\n4. Test with simple operations first\n\n---\n\n**After successful migration, the enhanced subjects controller will be fully functional with branch isolation and all advanced features!**\n"