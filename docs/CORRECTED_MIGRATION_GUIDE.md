# Corrected Migration Guide for Subjects Table\n\n## ✅ **Corrected Query**\n\nYou're absolutely right! The correct query is:\n\n```sql\nSELECT school_id, branch_id, branch_name FROM school_locations WHERE status = 'Active';\n```\n\n(Not `location_name` - it's `branch_name`)\n\n## 🚀 **Quick Migration Steps (Corrected)**\n\n### Step 1: Add Missing Columns\n\nExecute these one by one (ignore \"Duplicate column\" errors):\n\n```sql\nALTER TABLE subjects ADD COLUMN type VARCHAR(50) NOT NULL DEFAULT 'core' AFTER sub_section;\nALTER TABLE subjects ADD COLUMN is_elective BOOLEAN NOT NULL DEFAULT FALSE AFTER type;\nALTER TABLE subjects ADD COLUMN elective_group VARCHAR(50) NULL AFTER is_elective;\nALTER TABLE subjects ADD COLUMN weekly_hours DECIMAL(3,1) DEFAULT 0.0 AFTER elective_group;\nALTER TABLE subjects ADD COLUMN branch_id VARCHAR(20) NOT NULL DEFAULT '' AFTER weekly_hours;\nALTER TABLE subjects ADD COLUMN created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER branch_id;\nALTER TABLE subjects ADD COLUMN updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP AFTER created_at;\n```\n\n### Step 2: Add Primary Key and Indexes\n\n```sql\nALTER TABLE subjects ADD PRIMARY KEY (subject_code);\nCREATE INDEX idx_subjects_type ON subjects(type);\nCREATE INDEX idx_subjects_branch_id ON subjects(branch_id);\n```\n\n### Step 3: Check Available Branches (CORRECTED)\n\n```sql\nSELECT school_id, branch_id, branch_name, status \nFROM school_locations \nWHERE status = 'Active' \nORDER BY school_id, branch_id;\n```\n\n### Step 4: Check Subjects Needing Branch ID\n\n```sql\nSELECT school_id, COUNT(*) as subject_count \nFROM subjects \nWHERE branch_id = '' OR branch_id IS NULL \nGROUP BY school_id;\n```\n\n### Step 5: Update Branch IDs\n\n#### Option A: Manual Assignment (Recommended)\n```sql\n-- Replace with your actual school and branch IDs from Step 3\nUPDATE subjects SET branch_id = 'BRCH00001' WHERE school_id = 'SCH/1' AND (branch_id = '' OR branch_id IS NULL);\nUPDATE subjects SET branch_id = 'BRCH00002' WHERE school_id = 'SCH/2' AND (branch_id = '' OR branch_id IS NULL);\n-- Add more as needed...\n```\n\n#### Option B: Automatic Assignment\n```sql\n-- Assigns first available branch per school\nUPDATE subjects s\nSET s.branch_id = (\n    SELECT MIN(sl.branch_id)\n    FROM school_locations sl\n    WHERE sl.school_id = s.school_id\n    AND sl.status = 'Active'\n    LIMIT 1\n)\nWHERE s.branch_id = '' OR s.branch_id IS NULL;\n```\n\n### Step 6: Verify Success\n\n```sql\n-- Should return 0\nSELECT COUNT(*) as subjects_without_branch_id \nFROM subjects \nWHERE branch_id = '' OR branch_id IS NULL;\n\n-- Show distribution with branch names\nSELECT \n    s.school_id,\n    s.branch_id,\n    sl.branch_name,\n    COUNT(*) as subject_count\nFROM subjects s\nLEFT JOIN school_locations sl ON s.school_id = sl.school_id AND s.branch_id = sl.branch_id\nWHERE s.branch_id IS NOT NULL AND s.branch_id != ''\nGROUP BY s.school_id, s.branch_id, sl.branch_name\nORDER BY s.school_id, s.branch_id;\n```\n\n### Step 7: Check Table Structure\n\n```sql\nDESCRIBE subjects;\n```\n\n## 📁 **Corrected Files**\n\n1. **`CORRECTED_QUICK_FIX.sql`** - Complete quick fix with correct queries\n2. **`CORRECTED_UPDATE_BRANCH_IDS.sql`** - Detailed branch ID assignment with correct column names\n\n## ✅ **Expected Results**\n\nAfter running the corrected migration:\n\n1. ✅ All missing columns added to subjects table\n2. ✅ All subjects have valid branch_id values\n3. ✅ Enhanced controller works without \"Unknown column 'type'\" errors\n4. ✅ Branch isolation functions properly\n\n## 🎯 **Test the Fix**\n\nAfter migration, test the enhanced controller:\n\n```bash\ncurl -X POST \"http://localhost:34567/subjects\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"query_type\": \"create\",\n    \"subject\": \"Test Subject\",\n    \"section\": \"PRIMARY\",\n    \"class_code\": \"P1A\",\n    \"type\": \"core\"\n  }'\n```\n\n**Expected**: Success response without any column errors!\n\n---\n\n**Thank you for the correction! The migration scripts now use the correct `branch_name` column.**\n"