# Manual Migration Steps (No INFORMATION_SCHEMA Access)\n\n## 🚨 Issue\n\n**Error**: `#1044 - Access denied for user 'root'@'localhost' to database 'information_schema'`\n\n**Solution**: Use simplified migration scripts that don't require INFORMATION_SCHEMA access.\n\n## 🔧 Simple Migration Process\n\n### Step 1: Backup Your Database\n```bash\nmysqldump -u root -p your_database_name > subjects_backup.sql\n```\n\n### Step 2: Add Missing Columns\n\nExecute the statements in `SIMPLE_MIGRATE_SUBJECTS.sql` **one by one**.\n\n**Important**: You may see \"Duplicate column name\" errors - **this is normal** if some columns already exist. Just continue with the next statement.\n\n```sql\n-- Execute these one by one:\n\n-- 1. Add type column\nALTER TABLE subjects \nADD COLUMN type VARCHAR(50) NOT NULL DEFAULT 'core' \nCOMMENT 'Subject type: core, science, art, commercial, technology, vocational, health, language, selective'\nAFTER sub_section;\n\n-- 2. Add is_elective column\nALTER TABLE subjects \nADD COLUMN is_elective BOOLEAN NOT NULL DEFAULT FALSE \nCOMMENT 'Indicates if this subject is an elective course'\nAFTER type;\n\n-- 3. Add elective_group column\nALTER TABLE subjects \nADD COLUMN elective_group VARCHAR(50) NULL \nCOMMENT 'Group identifier for elective subjects'\nAFTER is_elective;\n\n-- 4. Add weekly_hours column\nALTER TABLE subjects \nADD COLUMN weekly_hours DECIMAL(3,1) DEFAULT 0.0 \nCOMMENT 'Weekly teaching hours for this subject'\nAFTER elective_group;\n\n-- 5. Add branch_id column (CRITICAL)\nALTER TABLE subjects \nADD COLUMN branch_id VARCHAR(20) NOT NULL DEFAULT '' \nCOMMENT 'Branch identifier - REQUIRED for multi-branch support'\nAFTER weekly_hours;\n\n-- 6. Add created_at timestamp\nALTER TABLE subjects \nADD COLUMN created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP \nCOMMENT 'Record creation timestamp'\nAFTER branch_id;\n\n-- 7. Add updated_at timestamp\nALTER TABLE subjects \nADD COLUMN updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP \nCOMMENT 'Record last update timestamp'\nAFTER created_at;\n```\n\n### Step 3: Add Primary Key and Indexes\n\n```sql\n-- Add primary key (ignore error if exists)\nALTER TABLE subjects ADD PRIMARY KEY (subject_code);\n\n-- Add indexes (ignore errors if they exist)\nCREATE INDEX idx_subjects_type ON subjects(type);\nCREATE INDEX idx_subjects_is_elective ON subjects(is_elective);\nCREATE INDEX idx_subjects_branch_id ON subjects(branch_id);\nCREATE INDEX idx_subjects_school_branch_status ON subjects(school_id, branch_id, status);\n```\n\n### Step 4: Add Type Constraint\n\n```sql\n-- Add constraint (ignore error if exists)\nALTER TABLE subjects \nADD CONSTRAINT chk_subject_type \nCHECK (type IN ('core', 'science', 'art', 'commercial', 'technology', 'vocational', 'health', 'language', 'selective'));\n```\n\n### Step 5: Update Branch IDs (CRITICAL)\n\n**First, check what branches are available:**\n```sql\nSELECT school_id, branch_id, location_name, status \nFROM school_locations \nWHERE status = 'Active' \nORDER BY school_id, branch_id;\n```\n\n**Then update subjects with appropriate branch_id:**\n\n#### Option A: Manual Assignment (Recommended)\n```sql\n-- Replace with your actual school and branch IDs\nUPDATE subjects SET branch_id = 'BRCH00001' WHERE school_id = 'SCH/1' AND (branch_id IS NULL OR branch_id = '');\nUPDATE subjects SET branch_id = 'BRCH00002' WHERE school_id = 'SCH/2' AND (branch_id IS NULL OR branch_id = '');\n-- Add more as needed...\n```\n\n#### Option B: Automatic Assignment\n```sql\n-- Assigns first available branch per school\nUPDATE subjects s\nSET s.branch_id = (\n    SELECT MIN(sl.branch_id)\n    FROM school_locations sl\n    WHERE sl.school_id = s.school_id\n    AND sl.status = 'Active'\n    LIMIT 1\n)\nWHERE s.branch_id IS NULL OR s.branch_id = '';\n```\n\n### Step 6: Verify Migration\n\n```sql\n-- Check table structure\nDESCRIBE subjects;\n\n-- Verify all subjects have branch_id (should return 0)\nSELECT COUNT(*) as subjects_without_branch_id\nFROM subjects \nWHERE branch_id IS NULL OR branch_id = '';\n\n-- Show sample data\nSELECT * FROM subjects LIMIT 3;\n```\n\n## 🎯 Expected Results\n\n### Before Migration:\n```\n+---------------+---------------+------+-----+---------+-------+\n| Field         | Type          | Null | Key | Default | Extra |\n+---------------+---------------+------+-----+---------+-------+\n| subject_code  | varchar(50)   | NO   |     | NULL    |       |\n| subject       | varchar(100)  | NO   |     | NULL    |       |\n| school_id     | varchar(11)   | NO   | MUL | NULL    |       |\n| status        | enum(...)     | YES  | MUL | Active  |       |\n| section       | varchar(45)   | NO   | MUL | NULL    |       |\n| sub_section   | varchar(50)   | YES  |     | NULL    |       |\n| class_code    | varchar(50)   | NO   |     | NULL    |       |\n+---------------+---------------+------+-----+---------+-------+\n```\n\n### After Migration:\n```\n+---------------+---------------+------+-----+---------+-------+\n| Field         | Type          | Null | Key | Default | Extra |\n+---------------+---------------+------+-----+---------+-------+\n| subject_code  | varchar(50)   | NO   | PRI | NULL    |       |\n| subject       | varchar(100)  | NO   |     | NULL    |       |\n| school_id     | varchar(11)   | NO   | MUL | NULL    |       |\n| status        | enum(...)     | YES  | MUL | Active  |       |\n| section       | varchar(45)   | NO   | MUL | NULL    |       |\n| sub_section   | varchar(50)   | YES  |     | NULL    |       |\n| type          | varchar(50)   | NO   | MUL | core    |       |\n| is_elective   | tinyint(1)    | NO   | MUL | 0       |       |\n| elective_group| varchar(50)   | YES  | MUL | NULL    |       |\n| weekly_hours  | decimal(3,1)  | YES  |     | 0.0     |       |\n| branch_id     | varchar(20)   | NO   | MUL | NULL    |       |\n| class_code    | varchar(50)   | NO   |     | NULL    |       |\n| created_at    | timestamp     | NO   |     | CURRENT |       |\n| updated_at    | timestamp     | NO   |     | CURRENT |       |\n+---------------+---------------+------+-----+---------+-------+\n```\n\n## 🚨 Troubleshooting\n\n### Common Errors and Solutions\n\n**1. \"Duplicate column name 'type'\"**\n- ✅ **Normal** - column already exists, continue with next statement\n\n**2. \"Multiple primary key defined\"**\n- ✅ **Normal** - primary key already exists, continue\n\n**3. \"Duplicate key name 'idx_subjects_type'\"**\n- ✅ **Normal** - index already exists, continue\n\n**4. \"Cannot add NOT NULL column\"**\n- Try adding as nullable first:\n  ```sql\n  ALTER TABLE subjects ADD COLUMN type VARCHAR(50) DEFAULT 'core';\n  UPDATE subjects SET type = 'core' WHERE type IS NULL;\n  ALTER TABLE subjects MODIFY type VARCHAR(50) NOT NULL DEFAULT 'core';\n  ```\n\n**5. \"Subjects still without branch_id\"**\n- Run the branch_id update queries\n- Check that school_locations table has active branches\n- Manually assign branch_id values\n\n## ✅ Success Verification\n\nAfter migration, test the enhanced controller:\n\n```bash\n# Test subject creation\ncurl -X POST \"http://localhost:34567/subjects\" \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"query_type\": \"create\",\n    \"subject\": \"Test Subject\",\n    \"section\": \"PRIMARY\",\n    \"class_code\": \"P1A\",\n    \"type\": \"core\"\n  }'\n```\n\n**Expected**: No \"Unknown column 'type'\" errors!\n\n## 📞 Final Notes\n\n- **Ignore duplicate column/key errors** - they're expected\n- **Every subject MUST have a branch_id** - this is critical\n- **Test thoroughly** after migration\n- **Keep your backup** until you're sure everything works\n\nAfter successful migration, restart your Node.js application and test all subject operations!\n"